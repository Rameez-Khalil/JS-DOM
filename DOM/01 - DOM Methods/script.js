//getElementById is a method.
//innerHTML is a property, useful for getting or setting the content within the HTML element.

const reference = (document.querySelector("#demo").innerHTML =
  "Rameez is the greatest");
